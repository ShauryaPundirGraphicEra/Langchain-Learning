There are two types of LLM's 
1) Which can generate the structured output:-
  For them we have the Structured_output module in langchain 

2)Which cannot generate structured output(They are generally the Open source llm):-
   for them we have the output parser module in the langchain 



Note: the output parser can generate the structured output fot both types of llms