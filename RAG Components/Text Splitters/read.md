Process of breaking large chunks of text like article ,PDF,HTML pages or books into smaller managable ,piecesor chunks that LLM can understand effectively


->Overcoming model limitation:-
   LLM has some context length limit,and if we want tot send he pdf to llm and want to summarize it ,but tit comntians 1000pages ,so we have to use the text splitters

-> Task like Embedding on a very large text ,results in poor embedding because we want to accomodate the large text into small embedding whihc doest work,instead splitting into difernet chunks would be better 

-> Types of text splitters
 1) Length based
 2) Text Structure based 
 3) Document Structure based
 4) Sementic Meaning based

 